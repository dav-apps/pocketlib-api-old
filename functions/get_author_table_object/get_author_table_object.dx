(# params: uuid: string)
(catch (
	(var author (TableObject.get uuid))
) (
	(# Access not allowed)
	(func render_validation_errors ((list (hash (error (get_error 1102)) (status 403)))))
))

(if (is_nil author) (
	(return nil)
) else (
	(# Check if the table object is an author)
	(if (author.table_id != env.AUTHOR_TABLE_ID) (
		(# Action not allowed)
		(func render_validation_errors ((list (hash (error (get_error 1102)) (status 403)))))
	))

	(return author)
))